This project is for developing a trekking Management Application for MAD-1 where we are trying to build a Trekking Management Application web application that allows Admin, Trek Staff, and Users (Trekkers) to interact with the system based on their roles.


MILESTONE -0
create a folder then open it with code 
create a virtual environment in terminal
        python -m venv .myenv
        .myenv/scripts/activate
create readme.md file for documenting 
INSTALLATION 
pip install flask
pip install flask-sqlalchemy
pip freeze > requiement.txt 


